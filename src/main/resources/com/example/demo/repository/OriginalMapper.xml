<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.OriginalMapper">
<insert id="insert">
  Insert INTO original (id,name,condition_id,category_name,brand,price,shipping,description)
  values (#{id},#{name},#{conditionId},#{categoryName},#{brand},#{price},#{shipping},#{description})
</insert>
</mapper>